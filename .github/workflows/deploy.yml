name: CI/CD Frontend Vite

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      statuses: write
      pull-requests: write
      
    env:
      VITE_API_URL: http://127.0.0.1:4000
      VITE_PEXELS_API_KEY: ${{ secrets.VERCEL_PEXELS_API_KEY }}

    steps:
      - name: Checkout Frontend repository code
        uses: actions/checkout@v6

      - name: Setup Node.js environment
        uses: actions/setup-node@v6
        with:
          node-version: '24.18.0'
          cache: 'npm'

      - name: Install Frontend dependencies
        run: npm ci

      - name: Install Playwright multi-browsers
        run: npx playwright install --with-deps

      # 🚀 STEP C: Clone and execute your Express Backend directly inside the workflow shell
      - name: Clone and run Express Backend companion server
        run: |
          git clone https://x-token-auth:${{ secrets.BACKEND_TOKEN }}@github.com/Catevika/2026-blog-app-backend.git backend-server
          cd backend-server
          
          echo "📦 Installing backend dependencies..."
          npm ci
          
          echo "🏗️ Building backend TypeScript source code..."
          npm run build
          
          echo "🚀 Launching companion backend server on unified IPv4 host..."
          NODE_ENV=production PORT=4000 HOST=0.0.0.0 MONGO_URI="${{ secrets.MONGO_URI }}" ACCESS_TOKEN_SECRET="${{ secrets.ACCESS_TOKEN_SECRET }}" REFRESH_TOKEN_SECRET="${{ secrets.REFRESH_TOKEN_SECRET }}" npm start &
          
          echo "⏳ Waiting for local backend server loop to bind on port 4000..."
          npx wait-on http://localhost:4000/api/health --timeout 30000
          echo "✅ Local Companion Backend is active and connected to MongoDB!"

      - name: Build Vite application for E2E testing
        run: npm run build

      - name: Run Playwright cross-browser tests (Chromium, Firefox, WebKit)
        run: npm run test:e2e
        env:
          CI: true 

      - name: Upload Playwright matrix report artifact
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      # ----------------------------------------------------
      # DEPLOYMENT PHASE (Only triggers on push to main branch)
      # ----------------------------------------------------
      - name: Verify Vercel deployment credentials presence
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ] || [ -z "${{ secrets.VERCEL_ORG_ID }}" ] || [ -z "${{ secrets.VERCEL_PROJECT_ID }}" ]; then
            echo "❌ Error: One or more Vercel deployment secrets are missing."
            exit 1
          fi
          echo "✅ Vercel configuration keys found in GitHub."

      - name: Install Vercel CLI globally
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: npm install --global vercel@latest

      - name: Pull Vercel environment configurations
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          echo "🔄 Validating credentials and linking environment to Vercel..."
          # 🚀 FIXED: Using official --scope syntax targeting your verified slug
          if ! vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }} --scope=${{ secrets.VERCEL_ORG_ID }} --project=${{ secrets.VERCEL_PROJECT_ID }}; then
            echo "❌ Error: Vercel authentication failed!"
            exit 1
          fi
        env:
          VERCEL_TELEMETRY_DISABLED: 1

      - name: Build production artifacts for Vercel
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }} --scope=${{ secrets.VERCEL_ORG_ID }} --project=${{ secrets.VERCEL_PROJECT_ID }}
        env:
          VITE_API_URL: https://two026-blog-app-backend.onrender.com
          VERCEL_TELEMETRY_DISABLED: 1

      - name: Deploy prebuilt artifacts to Vercel
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }} --scope=${{ secrets.VERCEL_ORG_ID }} --project=${{ secrets.VERCEL_PROJECT_ID }}
        env:
          VERCEL_TELEMETRY_DISABLED: 1
